## What it is

The asset metadata lookup Action.

## Why it exists

Agents need safe asset metadata retrieval.

## How to use it

Compose it through the built-in tools plugin.

## How it works

It resolves a canonical asset ID and reads the content store.
