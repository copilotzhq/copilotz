# Persistent Terminal Tool Resource

## What it is

The data-only presentation for a Persistent Terminal Action.

## Why it exists

Agent-facing Tool metadata must remain separate from executable terminal work.

## How to use it

The plugin factory creates it under the configured Tool alias.

## How it works

It binds the alias and stable presentation to the generated Action.
