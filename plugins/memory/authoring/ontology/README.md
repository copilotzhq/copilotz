# Memory ontology

## What it is

Public semantic-memory forms, kinds, and lifecycle declarations.

## Why it exists

Applications extend memory semantics without changing runtime behavior.

## How to use it

Register `defineMemoryKind(...)` values in resources.

## How it works

The declarations constrain consolidation parsing and record lifecycle.
