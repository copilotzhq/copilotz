# Tool Result Policy Resource

Sets the per-result inline byte threshold and the maximum output and source
sizes for explicit historical Tool result reads. Applications can override the
`toolResults.default` value in their composed Core Resources.
