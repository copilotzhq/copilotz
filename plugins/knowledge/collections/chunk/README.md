# Knowledge chunk collection

## What it is

The searchable, embedding-backed segment derived from a Knowledge document.

## Why it exists

It makes document sections individually searchable and removable with their
parent document.

## How to use it

Read it through the composed `chunk` Collection; ingestion creates and deletes
chunks automatically.

## How it works

Each chunk belongs to one document and stores content, embedding, location, and
metadata.
