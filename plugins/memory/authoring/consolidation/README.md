# Consolidation

## What it is

Semantic-memory proposal parsing and rendering helpers.

## Why it exists

The model and runtime share one deterministic consolidation contract.

## How to use it

Use `parseConsolidateMemoryInput` before committing a proposal.

## How it works

It validates kinds, evidence, references, and lifecycle transitions.
